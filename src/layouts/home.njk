---
layout: base
---

<header class="hero">
  <div class="hero-content">
    <div class="intro">
      {{ content | safe }}
    </div>
  </div>
</header>

<section class="post-list__wrapper">
  <h2 class="post-list__heading">Últimes ressenyes</h2>

  {% set postlist = reviews.list | head(-10) %}
  {# {% for review in reviews %}
    {{review.title}}
  {% endfor %} #}
  {% include "posts-list.njk" %}

</section>

<a class="all-posts-link" href="{{ '/blog/' | url }}">View All Posts</a>